[ ] Models
  [ ] Create a User Table 
  [ ] Create a User_Roles Table
  [ ] Create a Roles Table

[ ] User Management
  [ ] Models
    [ ] Create a user table
    [ ] Create a user_roles table
    [ ] Create a roles table
  [ ] Views 
    [ ] Create a template for login form
    [ ] Create a template for home page 
    [ ] Create a template for admins to create users
    [ ] Create a template for admins to disable users 
  [ ] Controllers
    [ ] Create a handler for the login form
    [ ] Create a handler for the create users form
    [ ] Create a handler for the disable users for
 
[ ] History Archive
  [ ] Models
    [x] Create an attachment_type table
    [x] Populate the attachment_type table
    [x] Create a record_type table
    [x] Populate the record_type table
    [x] Create the source_archive table
    [x] Populate the source_archive table
    [x] Create a collection table
    [x] Populate the collection table
    [ ] Normalize the history_record table
      [x] Add attachment_type_id (fk) to history_record
      [x] Update attachment_type_id based on the current attachment_type
      [x] Remove attachment_type 
      [ ] Create a file_attachment table
      [ ] Move file_attachment to file_attachment_table
      [ ] Add file_attachment_id to history_record
      [ ] Remove file_attachment from history_record
      [x] Add record_type_id (fk) to history_record
      [x] Update record_type_id based on the current record_type
      [ ] Remove record_type 
      [x] Add source_archive_id to history_record
      [x] Update source_archive_id based on the current source_archive 
      [ ] Remove source_archive
      [x] Add collection_id to history_record
      [x] Update collection_id based on the current collection
      [ ] Remove collection 
    [x] Add deleted_at field to history_record - default NULL 
    [x] Create a record_status table
    [x] Populate the record_status table (in_progress, pending_approval, published, unpublished)
    [x] Add a record_status_id field to history_record table (default id of in_progress state)
    [x] Update record_status_id to id of published 
    [ ] Create functions for creating history records
    [ ] Create functions for transitioning between states for history_record
    [ ] Create functions for updating a history_record
    [ ] Create functions for saving media in a folder  
    [ ] Create functions for saving history_records
    [ ] Create function for deleting history_records  
    [ ] Create a handler for querying history_records
  [ ] Views
    [ ] Create a template for viewing history_records (50 at a time)
    [ ] Create a template for creating history_records
    [ ] Create a template for editing history_records
    [ ] Create a template for logging in
  [ ] Controllers
    [ ] Create a handler that shows users their in progress records
    [ ] Create a handler that saves history_records
    [ ] Create a handler that changes history_record from in_progess to pending_review
    [ ] Create a handler that publishes history_record
    [ ] Create a handler that unpublishes a history_record
    [ ] Create a handler that deletes a history_record
    [ ] Create a handler that rejects a history record

[ ] Prototyping
  [x] Connect to the sqlite database
  [x] Select information from a history_record
  [x] Display history_record in a html template
  [x] Run a server and view that record
  [ ] After users table has been created:
    [ ] Add a login template
    [ ] Add a function for authenticating users
    [ ] Add a function for creating users
  [x] Compile and run app on windows and linux
[ ] API  
  [x] Create GET /records for getting records
  [ ] Create GET /records?offset= for paginating records
  [ ] Create POST /records for creating record
  [ ] Create GET /records/:id for getting a single record  
  [ ] Create POST /records/:id for updating records
